spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    show-sql: true

logging:
  level:
    org:
      hibernate:
        SQL: trace

    com:
      querydsl:
        SQL: trace
